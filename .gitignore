# Dependencies
node_modules/
# Local data / memory
.workbuddy/
# OS files
.DS_Store
Thumbs.db
*.log
# 单词卡 sprite 图已走 CloudBase CDN（不进 GitHub 以免 Pages 构建失败；本地保留作备份）
assets/cards/
# 本地备份目录（资源/版本/state.json 备份，不进 GitHub）
backup/
# 密钥 / 云函数源码（含 GH_TOKEN、夸克 cookie 等，绝不进 GitHub；本地保留）
cloudbaserc.json
quark-proxy/
